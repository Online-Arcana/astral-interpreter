{
  "name": "astral-interpreter",
  "version": "0.1.0",
  "description": "LLM interpretation for astral-core calculations",
  "type": "module",
  "private": true,
  "license": "MIT",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist", "src/interpretation/corpus/data/xml", "vendor", "LICENSE"],
  "scripts": {
    "corpus:embed": "node scripts/embed-corpus-xml.mjs",
    "vendor:core": "npm --prefix vendor/astral-core run vendor:build && npm --prefix vendor/astral-core install --ignore-scripts && npm --prefix vendor/astral-core run build",
    "vendor:schema": "npm --prefix vendor/openai-schema install && npm --prefix vendor/openai-schema run build",
    "vendor:build": "npm run vendor:core && npm run vendor:schema",
    "check": "npm run corpus:embed && tsc -p tsconfig.json --noEmit",
    "build": "npm run corpus:embed && rm -rf dist && tsc -p tsconfig.build.json",
    "test": "node test/interpreter.mjs",
    "ci": "npm run check && npm run build && npm test"
  },
  "dependencies": {
    "astral-core": "file:vendor/astral-core",
    "openai-schema": "file:vendor/openai-schema"
  },
  "devDependencies": { "@types/node": "^22.15.3", "typescript": "5.9.3" }
}
