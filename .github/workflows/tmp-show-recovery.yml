name: tmp-show-recovery
on:
  push:
    branches: [agent/split-modules]
    paths: [.github/workflows/tmp-show-recovery.yml]
permissions:
  contents: read
jobs:
  show:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: sed -n '360,570p' src/llm/orchestrate/run-v2.ts
